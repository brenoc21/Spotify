import styled from "styled-components";

export const DateInputBg = styled.div`
  display: flex;
  flex-direction: column;
  gap: 1rem;
  label {
    font-weight: 400;
    color: var(--dark-color);
  }
  input {
    font-weight: 400;
    padding: 0.8rem;
    color: var(--dark-color);
  }
`;
